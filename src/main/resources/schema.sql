DROP TABLE IF EXISTS test_counters;

CREATE TABLE test_counters
(
    id      BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name    VARCHAR(255)   NOT NULL,
    counter NUMERIC(19, 4) NOT NULL
);